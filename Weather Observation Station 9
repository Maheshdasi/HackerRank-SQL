SELECT CITY
FROM STATION
WHERE CITY NOT LIKE 'A%' OR 
      CITY NOT LIKE 'E%' OR
      CITY NOT LIKE 'I%' OR
      CITY NOT LIKE 'O%' OR
      CITY NOT LIKE 'U%';

--------------------------------------------

SELECT DISTINCT CITY
FROM STATION
WHERE NOT REGEXP_LIKE(CITY, '^[AEIOU]');
